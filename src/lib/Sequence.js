const Sequence = function (result, region, start, end) {
    this.dna = result
    this.chromosome = region
    this.start = start
    this.end = end
  }
  // Methods
  // TODO add low level methods
  Object.assign(Sequence.prototype, {
  
  })
  
  module.exports = Sequence
  