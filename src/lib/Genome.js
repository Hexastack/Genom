const Genome = function (result, region, start, end) {
    this.sequence = result
    this.chromosome = region
    this.start = start
    this.end = end
  }
  // Methods
  // TODO add low level methods
  Object.assign(Genome.prototype, {
  
  })
  
  module.exports = Genome
  